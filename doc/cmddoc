command list:

	cls
		画面に出力された文字列をクリアします

	del
		del <fname>
		ファイルを削除します

	dir
		存在するすべてのファイルを表示します

	exit
		コンソールを終了します

	langmode
		langmode <modenum>
		言語モードを変更します
		0=ASCII 1=SHIFT-JIS 2=EUC_JP

	mem
		メモリ総合量、メモリ使用可能量を表示

	neofetch
		環境の表示を行います。

	ncst
		コンソールを起動せず、アプリケーションを実行します
		起動時に使用したコンソールは入力可能になります

	rename
		rename <fname1> <fname2>
		ファイル名を変更します

	shutdown
		システムを終了します

	start
		コンソールを起動し、アプリケーションを実行します
