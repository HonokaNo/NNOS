command list:

	cls
		画面に出力された文字列をクリアします

	dir
		存在するすべてのファイルを表示します

	exit
		コンソールを終了します

	langmode
		言語モードを変更します

	mem
		メモリ総合量、メモリ使用可能量を表示

	neofetch
		環境の表示を行います。

	ncst
		コンソールを起動せず、アプリケーションを実行します
		起動時に使用したコンソールは入力可能になります

	start
		コンソールを起動し、アプリケーションを実行します
